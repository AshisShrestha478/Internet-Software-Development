CMD:
nslookup google.com			// look up internet server address and the one we requesting for

ipconfig				// See your device Ip address

protocol: what type of request is made

Domain name and subdomain is go for the nslookup/ where we sending the request to

Port: there will be alot of server but where is it going to land which server
        they are 16 bits, well known port or reserved port has 1000 spot reserved   

http default port is 80
https default port is 443

If we use private network then how come that i can communicate with the public site?
    NAT/PAT does it and router assign private address to device it is connected

Path: identify the resource

query will filter out the data basically meta data and is done using ?para = parameter1

200s are success
300s are redirect (it say reissue our address to correct place)
400s are failure (client error)
500s are failure server error

cookies help keep up the activities you did on that website
